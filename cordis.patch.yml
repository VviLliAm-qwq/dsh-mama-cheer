# Your patch layer for this dsh profile, applied after every bundle layer:
# a top-level YAML array of loader patch entries (id-targeted config
# overrides, disables, and insert lists; `!!js` expressions allowed).
#
# dsh-mama-cheer: one keyboard shortcut that drops an encouraging message into
# the live conversation. It needs no permissions (no decision-event
# interception, no storage): it reads the live Agent from the public
# `ctx.agents` registry and delivers through the agent's own inbox API.
- insert:
    - id: dsh-mama-cheer
      name: 'dsh-mama-cheer'
